{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserType list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Name</th>
                <th>Code</th>
                <th>Usertypeid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('usertype_show', { 'id': entity.userTypeId }) }}">{{ entity.name }}</a></td>
                <td>{{ entity.code }}</td>
                <td>{{ entity.userTypeId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('usertype_show', { 'id': entity.userTypeId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('usertype_edit', { 'id': entity.userTypeId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('usertype_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
