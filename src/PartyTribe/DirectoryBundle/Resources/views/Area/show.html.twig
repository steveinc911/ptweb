{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Area</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Areaid</th>
                <td>{{ entity.areaId }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('area') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('area_edit', { 'id': entity.areaId }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
