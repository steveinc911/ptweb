{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EventArtist list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Eventartistid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('eventartist_show', { 'id': entity.eventArtistId }) }}">{{ entity.eventArtistId }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('eventartist_show', { 'id': entity.eventArtistId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('eventartist_edit', { 'id': entity.eventArtistId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('eventartist_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
