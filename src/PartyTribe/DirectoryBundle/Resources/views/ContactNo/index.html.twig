{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ContactNo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Number</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('contactno_show', { 'id': entity.number }) }}">{{ entity.number }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('contactno_show', { 'id': entity.number }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('contactno_edit', { 'id': entity.number }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('contactno_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
